# Streamlit (會自動安裝)
streamlit

# 數據處理與科學計算 (numpy must be first for insightface build)
numpy>=1.23,<2.0

# 人臉辨識
insightface>=0.7.3
albumentations>=1.3.0
onnxruntime>=1.15.0,<1.20.0

# 圖像處理（使用 headless 版本避免 GUI 依賴）
opencv-python-headless>=4.8.0
pillow>=10.0.0

scipy>=1.11.0
scikit-learn>=1.3.0
scikit-image>=0.21.0

# 其他依賴
matplotlib>=3.7.0
tqdm>=4.65.0
requests>=2.31.0
onnx>=1.15.0
easydict>=1.10
